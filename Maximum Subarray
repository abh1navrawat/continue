Given an integer array nums, find the subarray with the largest sum, and return its sum.



intuition - Using dynamic programming. Change the original array, if the sum of curr and prev value is bigger than the curr value, update nums. ans will store the biggest value of the updated nums. return that value
            intially the ans will store the first value of nums because if all the values are negative it will not take any value and the self determined value will come as the answer.






class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        ans=nums[0]
        for i in range(1,len(nums)):
            nums[i] = max(nums[i],nums[i]+nums[i-1])
            ans = max(nums[i],ans)
        return ans
