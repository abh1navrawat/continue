class Solution:
    def isValid(self, s: str) -> bool:
        pict=dict(('()','[]','{}'))
        stack=[]
        for i in s:
            if i in '([{':
                stack.append(i)
            elif len(stack) == 0 or i != pict[stack.pop()]:
                return False
        return len(stack) ==0
