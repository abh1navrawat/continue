class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        i=0
        nums.sort()
        for n in nums:
            if n != i:
                break
            i+=1
        return i
